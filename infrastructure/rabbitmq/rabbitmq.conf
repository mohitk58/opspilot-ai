# Per-queue metric series (rabbitmq_queue_messages{queue="q.notifications.dlq"})
# instead of node-level aggregates — the DLQ alert needs the queue dimension.
prometheus.return_per_object_metrics = true
